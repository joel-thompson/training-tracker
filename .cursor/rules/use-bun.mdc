---
alwaysApply: true
---

use bun to run commands. you may need to run nvm use to use the correct version of node.

do not use bunx or npx to run linting or typechecking.

examples:
nvm use && bun lint
nvm use && bun typecheck
cd frontend && nvm use && bun lint